<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campuspe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusPe">
        <activity android:name=".ui.RewardActivity"></activity>
        <activity android:name=".ui.login.LoginName" />
        <activity android:name=".ui.CanteenActivity" />
        <activity
            android:name=".UserProfile"
            android:exported="true"
            android:label="Profile"
            android:parentActivityName=".ui.LoggedIn" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoggedIn" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.PaymentActivity"
            android:label="Payment" />
        <activity
            android:name=".ui.PaymentStatus"
            android:label="Payment Status"
            android:parentActivityName=".ui.LoggedIn" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>